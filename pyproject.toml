[project]
name = "linux-test-repo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard,standart]>=0.133.1",
    "google-genai>=1.65.0",
]
